{
  "name": "clockster-bitrix-sync",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "audit": "node src/auditComparison.js",
    "audit:realization": "node src/auditRealizationComparison.js",
    "rename:realization": "node src/renameByRealization.js",
    "dry-run": "node src/index.js --dry-run",
    "sync": "node src/index.js --sync",
    "bitrix:test": "node src/index.js --bitrix-only"
  },
  "engines": {
    "node": ">=20"
  }
}
